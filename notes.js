// some == atlist one value pass in the codition that time we use some method
// findIndex = method same as like find method .but this method search only index of result value
// every == method return onlu boolen value.. and every method accepting arrguments(comperison operators)
// includes == method return boolen value ..search keyword in include methods
// find == method return only first value of the result ...and accept arrguments
// fill == all element value convert in arrgument value like - a so all array inside [a,a,a,a,a]
// sort method use only alphabaticaly/numerically - if i have an array inside the string value then i dont write a arrgument in sort method otherwise i store data in numerical value so that time i pass argument in sort method for data sorting or data reverse sorting ,...

//... new

// Diffrence
// findIndex = mehtod return the element index / first index
// find = method return the element value / first value

// Diffrence
// map = method return a new array / method used to transform the element of an array
// forech = method does not a return array / method used to loop through the element of an array / method call a function for each element of an array

// Diffrence
// filter = method return a array to containing the element that setisfied the condition
// find = method does not return array and return itself element that setisfied the condition

// shift = method use for first index delete of an array
// pop = method use for delete the last of index an array
// push = method use for adding the element of the last index of an array
// unshift = method use for adding new element of the zeroeth index of an array
// join = method use for joining the array element into a string
// concat = method use for concatination/merge two or more then two arrays
// splice = method use for romving/replacing element of existing array
// slice = method use for get a element value and return a new array 

// sort = method use for element sort alphabeticaly 
// reverse = method use for reversing the element in an array
// foreach = 
// every = method check the all array value pass the test then return true or false
// indexof = method searching the all array element value and return the value index
// find = method use for returns the value of the first element that passes a test
// find = method return the first value of the array element that passes a test
// findindex = method return first index of array element that passes a test
// includes = method checking the element is present in the array


// find = method return the first value of the array element that passes a test
// findindex = method return first index of array element that passes a test
// includes = method checking the element is present in the array